[package]
name = "jwt_recovery"
type = "bin"
authors = [""]
compiler_version = ">=1.0.0"

[dependencies]
jwt = { tag = "v0.5.1", git = "https://github.com/zkemail/noir-jwt" }
keccak256 = { tag = "v0.1.3", git = "https://github.com/noir-lang/keccak256" }
